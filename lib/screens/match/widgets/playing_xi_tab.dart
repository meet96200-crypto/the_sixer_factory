import 'package:flutter/material.dart';

class PlayingXITab extends StatelessWidget {
  const PlayingXITab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Playing XI",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}